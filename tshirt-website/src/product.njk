---
layout: layout.njk
title: ComfyTees | Products
---

<section class="products-index">
  <h1 class="section-title">All Products</h1>
  <p class="lead">Browse our curated collection of comfortable, eco-friendly tees.</p>

  <div class="product-grid">
    {% for p in products %}
    <div class="product-card">
      <img src="{{ p.image }}" alt="{{ p.alt }}">
      <h3>{{ p.title }}</h3>
      <p class="product-price">${{ p.price }}</p>
      <a href="/product/{{ p.slug }}/" class="btn btn-secondary btn-small">View Details</a>
    </div>
    {% endfor %}
  </div>
</section>
